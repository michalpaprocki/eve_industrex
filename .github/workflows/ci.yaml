name: CI

on:
  push:
    branches:
      - ci-learning
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print repository contents
        run: |
          pwd
          ls -la